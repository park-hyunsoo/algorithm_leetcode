# 문자열 리스트 역순 정렬 문제
# 파이썬 내장 함수를 사용
# 투 포인트를 사용해서 맞교환 

class Solution:
    def reverseString(self, s: List[str]) -> None:
        left, right = 0, len(s) -1
        while left < right:
            s[left],s[right] = s[right], s[left]
            left += 1
            right -= 1

    #def reverseString(self, s: List[str]) -> None:
    #    s.reverse()
